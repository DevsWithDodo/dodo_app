const String APP_URL =  'https://www.examp.le/api';
const String TEST_URL = 'https://test.examp.le/api';
int currentVersion=YOUR_CURRENT_VERSION;
bool useTest = false;

Map<String, String> adUnitIds = {
  'create_group':'example',
  'history':'example',
  'home_screen':'example',
  'join_group':'example',
  'payment':'example',
  'purchase':'example',
  'report_bug':'example',
  'settings':'example'
};

/// Whether in app purchase enabled on the platform. Set programmatically.
bool isIAPPlatformEnabled = false;

/// Whether ads are enabled on the platform. Set programmatically.
bool isAdPlatformEnabled = false;

/// Whether Firebase are enabled on the platform. Set programmatically.
bool isFirebasePlatformEnabled = false;

double tabletViewWidth = 850;
double adHeight(BuildContext context) => (isAdPlatformEnabled && (context.read<UserProvider>().user?.showAds ?? false)) ? 50 : 0;